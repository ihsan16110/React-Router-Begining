import React from 'react';
import Header from '../Header/Header';

const Home = () => {
    return (
        <div>
            <Header></Header>
            <h4>This is the Real Home Page</h4>
        </div>
    );
};

export default Home;